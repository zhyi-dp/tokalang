import core/types::{Addr, usize, ADDR0}

pub fn main() -> i32 {
    auto a: Addr = ADDR0
    if a != ADDR0 {
        return 1
    }
    auto u_a = a as usize
    if u_a != 0 {
        return 2
    }
    return 0
}
