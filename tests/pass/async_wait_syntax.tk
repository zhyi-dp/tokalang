import std/task
import std/task::{block_on, __toka_spawn_blocking}

fn fetch() -> async str {
    return "hello"
}

fn read_config() -> wait str {
    return "config"
}

fn pure_caller() {
    block_on<str>(fetch())
}

pub fn main() -> i32 {
    fetch().start
    pure_caller()
    return 0
}
