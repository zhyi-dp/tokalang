import core/traits::{@PartialOrd}
import core/primitives

pub fn cmp_val<'T: @PartialOrd>(a: 'T, b: 'T) -> i32 {
    return a.cmp(b)
}

pub fn main() -> i32 {
    auto a = 10
    auto b = 20
    assert(cmp_val(a, b) == -1, "10 < 20")
    
    return 0
}
