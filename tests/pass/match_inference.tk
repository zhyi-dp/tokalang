import core/option::Option
import std/io::println

fn main() -> i32 {
    auto opt = Option<i32>::Some(42)
    match opt {
        auto Some(v) => { println("v={}", v) }
        auto None => { println("None") }
    }
    return 0
}
