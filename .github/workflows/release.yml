name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    name: Build & Release on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            name: linux-x64
            pkg_cmd: OS=linux ARCH=x64 ./tools/scripts/package_release.sh ${{ github.ref_name }}
          - os: macos-13
            name: macos-x64
            pkg_cmd: OS=macos ARCH=x64 ./tools/scripts/package_release.sh ${{ github.ref_name }}
          - os: macos-latest
            name: macos-arm64
            pkg_cmd: OS=macos ARCH=arm64 ./tools/scripts/package_release.sh ${{ github.ref_name }}
            
    steps:
      - uses: actions/checkout@v4

      - name: Install LLVM 20 (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          export DEBIAN_FRONTEND=noninteractive
          sudo -E apt-get update -o Acquire::Retries=5 || true
          wget --timeout=15 --tries=3 https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo -E ./llvm.sh 20
          
      - name: Install LLVM 20 (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install llvm@20 || brew install llvm

      - name: Build Toka (macOS/Linux)
        run: |
          mkdir build && cd build
          cmake .. -DCMAKE_BUILD_TYPE=Release
          make -j4
          
          # Build toka CLI tool and tokafmt using the newly built compiler
          export TOKA_LIB=$GITHUB_WORKSPACE/lib
          ./bin/tokac -I ../tools/toka ../tools/toka/src/main.tk -o toka.ll -O3
          ./bin/tokac -I ../tools/tokafmt ../tools/tokafmt/src/main.tk -o tokafmt.ll -O3
          
          if [ "${{ runner.os }}" = "macOS" ]; then
            export LLVM_PATH=$(brew --prefix llvm@20 || brew --prefix llvm)
            $LLVM_PATH/bin/clang toka.ll -isysroot $(xcrun --show-sdk-path) -lm -o ./bin/toka -O3
            $LLVM_PATH/bin/clang tokafmt.ll -isysroot $(xcrun --show-sdk-path) -lm -o ./bin/tokafmt -O3
          else
            clang-20 toka.ll -lm -o ./bin/toka -O3
            clang-20 tokafmt.ll -lm -o ./bin/tokafmt -O3
          fi
          
          rm toka.ll tokafmt.ll
      - name: Run Package Script
        shell: bash
        run: |
          ${{ matrix.pkg_cmd }}
          
      - name: Upload Release Asset
        uses: softprops/action-gh-release@v2
        with:
          files: build/toka-${{ github.ref_name }}-${{ matrix.name }}.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
